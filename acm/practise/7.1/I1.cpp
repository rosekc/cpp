#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
using namespace std;

int main()
{
	int n, m;
	int a, c;
	string b;

	while ( 1 )
	{
		cin >> n >> m;

		if ( m == 0 )
		{
			break;
		}

		struct st
		{
			int num;
			int name;
			int mark;
		}

		struct st table[ n ];


		for ( int i = 0; i < n; i++ )
		{
			cin >> a >> b >> c;
			table[ i ] = { a, b, c };
		}

		if ( m == 1 )
		{
			tmpint = av;
			sort( tmpint)
		}
	}

}
